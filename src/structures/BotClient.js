// consts
const { 
    Client,
    Collection,
    GatewayIntentBits,
    Partials,
    WebhookClient,
    ApplicationCommandType
} = require('discord.js');

const path = require('path');
const { table } = require('table');